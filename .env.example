# Required — Anthropic API key for all persona LLM calls.
ANTHROPIC_API_KEY=

# Optional — override default model.
ANTHROPIC_MODEL=claude-sonnet-4-6

# Optional — polite contact email sent in the User-Agent header to gov.uk APIs.
RADAR_CONTACT_EMAIL=
