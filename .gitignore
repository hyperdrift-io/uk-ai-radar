node_modules
.nuxt
.output
.env
.env.*
!.env.example
*~
*.log
.DS_Store

# Cache + outputs (keep the dirs)
data/*
!data/.gitkeep
digests/*
!digests/.gitkeep

# Local profiles (only the example is tracked)
profiles/*.json
!profiles/example.json
